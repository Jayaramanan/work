GIT : 

git config --global user.email

git config --global user.email "email@example.com"

Old : developer@vetsource.com

------------------------------------------
Slack :

sudo apt-get update
sudo apt-get install snapd
sudo snap install slack --classic
sudo apt purge snapd snap-confine && sudo apt install -y snapd (If any issue)

----------------------------------------------------------------------
Node:

sudo rm -rf /usr/local/bin/npm /usr/local/share/man/man1/node* /usr/local/lib/dtrace/node.d ~/.npm ~/.node-gyp /opt/local/bin/node /opt/local/include/node /opt/local/lib/node_modules 

sudo rm -rf /usr/local/lib/node*

sudo rm -rf /usr/local/include/node*

sudo rm -rf /usr/local/bin/node*

------------------------------------------------------------------------

TimeZone:

timedatectl list-timezones
sudo timedatectl set-timezone asia/calcutta

-----------------------------------------
Shared folder:

http://confluence.aws.vetsource.com/pages/viewpage.action?spaceKey=~nilay&title=Vetsource+Essentials#VetsourceEssentials-Createsharedfolderinwindowsvm

-------------------------------------------------------
Credential:

1. vetsource Jira:
	jkumar
	welcome@123
2. Slack
	jayaraman@asahitechnologies.com
	welcome@123
3. TeamViewer (unattended access)
	jayaraman@asahitechnologies.com
-------------------------------------------------------------------------------------


-------------------------------------------------------------------------------------------
Angular fire:

1. Observable issue : need to update the ngrx. Check angular change log.

npm i rxjs@^6.0 rxjs-compat
	welcome@123
------------------------------




